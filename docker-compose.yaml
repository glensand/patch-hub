version: "3.8"

services:
  phub:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1555:1555"
    volumes:
      - ./logs:/app/logs
      - ./cache:/app/cache
    restart: unless-stopped
